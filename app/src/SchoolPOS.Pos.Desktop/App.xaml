<Application x:Class="SchoolPOS.Pos.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <!-- Convertidores -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />

        <!-- Paleta y estilos base -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#1E5AA8" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2E8B57" />
        <SolidColorBrush x:Key="DangerBrush" Color="#C0392B" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="#F4F6F9" />

        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="MinWidth" Value="90" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Margin" Value="0,4" />
        </Style>
    </Application.Resources>
</Application>
