<Window x:Class="SchoolPOS.Pos.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Punto de Venta Escolar — Acceso"
        Height="440" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" Background="{StaticResource SurfaceBrush}">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Tienda Escolar" FontSize="26" FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" />
        <TextBlock Grid.Row="1" Text="Punto de Venta" FontSize="14" Foreground="#666"
                   HorizontalAlignment="Center" Margin="0,0,0,24" />

        <TextBlock Grid.Row="2" Text="Usuario" FontWeight="SemiBold" />
        <TextBox Grid.Row="3" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="14" />

        <StackPanel Grid.Row="4" Margin="0,8,0,0">
            <TextBlock Text="Contraseña" FontWeight="SemiBold" />
            <PasswordBox x:Name="PasswordBox" Padding="8,6" Margin="0,4" FontSize="14"
                         PasswordChanged="PasswordBox_OnPasswordChanged" />
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="0,16,0,0">
            <Button Content="Iniciar sesión" Style="{StaticResource PrimaryButton}"
                    Command="{Binding LoginCommand}" IsDefault="True" Height="44" FontSize="15" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                       TextWrapping="Wrap" Margin="0,10,0,0" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>
