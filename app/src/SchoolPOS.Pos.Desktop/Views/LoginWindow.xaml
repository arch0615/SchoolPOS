<Window x:Class="SchoolPOS.Pos.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LoncherApp — Punto de Venta · Acceso"
        Height="460" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" Background="{StaticResource SurfaceBrush}">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <Viewbox Width="58" Height="58" HorizontalAlignment="Center">
                <Canvas Width="64" Height="64">
                    <Path Data="M32 21 C 27 13, 17 14, 15 24 C 13 33, 18 51, 27 51 C 30 51, 30 49, 32 49 C 34 49, 34 51, 37 51 C 46 51, 51 33, 49 24 C 47 14, 37 13, 32 21 Z"
                          Stroke="#EFA43B" StrokeThickness="4.5" StrokeLineJoin="Round" />
                    <Path Data="M33 19 C 35 12, 41 9, 46 12" Stroke="#EFA43B" StrokeThickness="3.2"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                    <Path Data="M31 19 C 24 9, 15 11, 20 18 C 24 23, 30 22, 31 19 Z" Fill="#5FB03A" />
                </Canvas>
            </Viewbox>
            <TextBlock FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,4,0,0">
                <Run Text="Loncher" Foreground="#57A839" /><Run Text="App" Foreground="#F19234" />
            </TextBlock>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="Punto de Venta" FontSize="14" Foreground="{StaticResource MutedBrush}"
                   HorizontalAlignment="Center" Margin="0,2,0,24" />

        <TextBlock Grid.Row="2" Text="Usuario" FontWeight="SemiBold" />
        <TextBox Grid.Row="3" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="14" />

        <StackPanel Grid.Row="4" Margin="0,8,0,0">
            <TextBlock Text="Contraseña" FontWeight="SemiBold" />
            <PasswordBox x:Name="PasswordBox" Padding="8,6" Margin="0,4" FontSize="14"
                         PasswordChanged="PasswordBox_OnPasswordChanged" />
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="0,16,0,0">
            <Button Content="Iniciar sesión" Style="{StaticResource PrimaryButton}"
                    Command="{Binding LoginCommand}" IsDefault="True" Height="44" FontSize="15" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                       TextWrapping="Wrap" Margin="0,10,0,0" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>
