<UserControl x:Class="SchoolPOS.Pos.Desktop.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="24">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,12">
            <Viewbox Width="26" Height="26" Margin="0,0,10,0">
                <Canvas Width="64" Height="64">
                    <Path Data="M32 21 C 27 13, 17 14, 15 24 C 13 33, 18 51, 27 51 C 30 51, 30 49, 32 49 C 34 49, 34 51, 37 51 C 46 51, 51 33, 49 24 C 47 14, 37 13, 32 21 Z"
                          Stroke="#EFA43B" StrokeThickness="4.5" StrokeLineJoin="Round" />
                    <Path Data="M33 19 C 35 12, 41 9, 46 12" Stroke="#EFA43B" StrokeThickness="3.2"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                    <Path Data="M31 19 C 24 9, 15 11, 20 18 C 24 23, 30 22, 31 19 Z" Fill="#5FB03A" />
                </Canvas>
            </Viewbox>
            <TextBlock Text="Reportes" FontSize="24" FontWeight="Bold" Foreground="{StaticResource InkBrush}" VerticalAlignment="Center" />
        </StackPanel>

        <Grid DockPanel.Dock="Top" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                <TextBlock Text="Desde" />
                <DatePicker SelectedDate="{Binding From}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="0,0,12,0">
                <TextBlock Text="Hasta" />
                <DatePicker SelectedDate="{Binding To}" />
            </StackPanel>
            <Button Grid.Column="2" Content="Generar" Style="{StaticResource PrimaryButton}"
                    Command="{Binding RefreshCommand}" VerticalAlignment="Bottom" />
        </Grid>

        <!-- Tarjetas resumen -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,14">
            <Border Background="White" CornerRadius="8" Padding="18" Margin="0,0,12,0" Width="200">
                <StackPanel>
                    <TextBlock Text="Ventas del periodo" Foreground="{StaticResource MutedBrush}" />
                    <TextBlock Text="{Binding SalesTotalText}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" />
                    <TextBlock Text="{Binding Sales.SaleCount, StringFormat='{}{0} ventas'}" Foreground="{StaticResource MutedBrush}" />
                    <TextBlock Text="{Binding Sales.TotalByBalance, StringFormat='Saldo: {0:C2}'}" Foreground="{StaticResource MutedBrush}" FontSize="12" />
                    <TextBlock Text="{Binding Sales.TotalByCash, StringFormat='Efectivo: {0:C2}'}" Foreground="{StaticResource MutedBrush}" FontSize="12" />
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="8" Padding="18" Margin="0,0,12,0" Width="200">
                <StackPanel>
                    <TextBlock Text="Flujo de efectivo" Foreground="{StaticResource MutedBrush}" />
                    <TextBlock Text="{Binding CashFlow.Net, StringFormat=C2}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                    <TextBlock Text="{Binding CashFlow.ManualIncome, StringFormat='Ingresos: {0:C2}'}" Foreground="{StaticResource MutedBrush}" FontSize="12" />
                    <TextBlock Text="{Binding CashFlow.ManualExpense, StringFormat='Egresos: {0:C2}'}" Foreground="{StaticResource MutedBrush}" FontSize="12" />
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="8" Padding="18" Width="200">
                <StackPanel>
                    <TextBlock Text="Saldos de clientes" Foreground="{StaticResource MutedBrush}" />
                    <TextBlock Text="{Binding Balances.TotalBalance, StringFormat=C2}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource InkBrush}" />
                    <TextBlock Text="{Binding Balances.AccountCount, StringFormat='{}{0} cuentas'}" Foreground="{StaticResource MutedBrush}" />
                </StackPanel>
            </Border>
        </StackPanel>

        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,6">
            <TextBlock Text="Ventas por producto" FontWeight="SemiBold" FontSize="15" />
            <Button Content="Exportar CSV" Command="{Binding ExportProductsCommand}" HorizontalAlignment="Right" DockPanel.Dock="Right" />
        </DockPanel>
        <DataGrid ItemsSource="{Binding TopProducts}" AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="White" RowBackground="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Producto" Binding="{Binding Description}" Width="*" />
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Quantity}" Width="140" />
                <DataGridTextColumn Header="Ingreso" Binding="{Binding Revenue, StringFormat=C2}" Width="160" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
